public static int lengthOfLastWord(String s) {
        if (s.equals(null) || s.equals("")) return 0;
        String[] splitedInput = s.trim().split(" ");
        int lastWordIndex = splitedInput.length - 1;
        return splitedInput[lastWordIndex].length();
    }
